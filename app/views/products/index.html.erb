<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>cart / add</title>
  </head>
  <body>
    <% cart.each do |cart_item|%>
      <p>
        <%= cart_item %>
      </p>
    <%end%>

    <%= form_tag("/products/index") do %>
      Add item to cart: <%= text_field_tag :product %><br>
      <%= submit_tag "add to cart" %>
      <% end %>
  </body>
</html>
